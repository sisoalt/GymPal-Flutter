name: gympal
description: A new Flutter project.
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  
  # State Management
  provider: ^6.0.5

  # Database
  hive: ^2.2.3
  hive_flutter: ^1.1.0

  # Utilities
  intl: ^0.19.0
  path: ^1.9.0
  http: ^1.2.0

  # Media & Files
  image_picker: ^1.0.7
  path_provider: ^2.1.2

  # UI Icons (Optional but standard)
  cupertino_icons: ^1.0.6
  crypto: ^3.0.7

dev_dependencies:
  flutter_test:
    sdk: flutter
  
  # Code Generation
  build_runner: ^2.4.8
  hive_generator: ^2.0.1
  flutter_lints: ^3.0.1
flutter:
  uses-material-design: true

  assets:
    - assets/images/logo.png
